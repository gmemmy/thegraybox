import { View, StyleSheet } from 'react-native';

export default function ReactionsEmitter() {
  return <View style={styles.surface} />;
}

const styles = StyleSheet.create({
  surface: {
    flex: 1,
    backgroundColor: 'rgba(255, 128, 0, 0.08)',
  },
});


